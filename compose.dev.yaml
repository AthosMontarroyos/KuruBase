services:
  postgres:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    networks:
      - private
      - development

  api:
    ports:
      - "127.0.0.1:${API_PORT:-8080}:8080"

networks:
  development:
